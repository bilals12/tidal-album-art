import { PlaybackState } from '../../api/interfaces';

/**
 * Get the current playback state of the active player.
 *
 * @returns {PlaybackState}
 */
export declare function getPlaybackState(): PlaybackState;
